package gui.accounts_table.view;

import org.springframework.stereotype.Component;

import javax.swing.*;

@Component()
public class AccountsFrame extends JFrame {

    public void init() {
        setSize(500, 500);
        setLocationRelativeTo(null);
    }
}
